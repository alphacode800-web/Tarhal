# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment
.env
.env.*
!.env.example

# Uploaded media — project assets are tracked; only ignore stray temp files
public/uploads/*.tmp

# Server data & seed catalogs are tracked for full project deploy
# (see server/data/ and server/seed/)

# Root data/ duplicate exceeds GitHub 100MB limit — canonical copy lives in server/data/
data/car-vehicles.json

# SQLite WAL sidecar files (regenerated at runtime)
server/database/*.db-shm
server/database/*.db-wal
node_modules.lnk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
